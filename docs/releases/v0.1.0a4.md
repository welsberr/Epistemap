# Epistemap v0.1.0a4

Patch release for the `v0.1.0a3` MCP transport. The stdio server now reports
its actual release version in MCP `serverInfo`; graph and confidence formats
are unchanged.

Validation: Epistemap test suite passes with 165 tests.
